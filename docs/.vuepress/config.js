import { defineUserConfig } from 'vuepress'


export default defineUserConfig({
    plugins: [
    ],
    lang: 'zh-CN',
    title: '路的尽头在哪',
    description: '做人最重要的是开心',
    port: 80,//开发环境端口
    open: true,//启动调试后自动打开浏览器
})